#pragma once

// ============================================================
// BombInfo.h
// Bomb info function declarations.
// ============================================================

#include "Types.h"

// Render the persistent Bomb Info ImGui window (uses offsets + memory reads).
void RenderBombInfoWindow();
